You are a senior React Native / Expo / iOS engineer.
Apply ALL changes below in order. Do not skip any step.
The app is at: artifacts/mobile/

CRITICAL: The bundle ID for this app is com.zorockz.Yoga
NOT com.stretchgate.app. Every file must use com.zorockz.Yoga.
The App Group is group.com.zorockz.Yoga.
The extension bundle ID is com.zorockz.Yoga.DeviceActivityMonitor.

---

## STEP 1 — Fix app.json

Replace the entire file artifacts/mobile/app.json with:

{
  "expo": {
    "name": "StretchGate",
    "slug": "mobile",
    "version": "1.0.0",
    "orientation": "portrait",
    "icon": "./assets/images/icon.png",
    "scheme": "stretchgate",
    "userInterfaceStyle": "light",
    "newArchEnabled": true,
    "splash": {
      "image": "./assets/images/splash-icon.png",
      "resizeMode": "contain",
      "backgroundColor": "#0F2820"
    },
    "ios": {
      "supportsTablet": false,
      "bundleIdentifier": "com.zorockz.Yoga",
      "buildNumber": "1",
      "jsEngine": "hermes",
      "entitlements": {
        "com.apple.developer.family-controls": true,
        "com.apple.security.application-groups": [
          "group.com.zorockz.Yoga"
        ]
      }
    },
    "android": {},
    "web": {
      "favicon": "./assets/images/icon.png"
    },
    "plugins": [
      [
        "expo-router",
        {
          "origin": "https://replit.com/"
        }
      ],
      "expo-font",
      "expo-web-browser",
      [
        "expo-notifications",
        {
          "icon": "./assets/images/icon.png",
          "color": "#3A7A5C",
          "defaultChannel": "default"
        }
      ]
    ],
    "experiments": {
      "typedRoutes": true,
      "reactCompiler": true
    }
  }
}

---

## STEP 2 — Create app.config.js

CREATE the file artifacts/mobile/app.config.js:

const { withEntitlementsPlist, withInfoPlist } = require('@expo/config-plugins');

module.exports = ({ config }) => {
  config = withEntitlementsPlist(config, (cfg) => {
    cfg.modResults['com.apple.developer.family-controls'] = true;
    cfg.modResults['com.apple.security.application-groups'] = [
      'group.com.zorockz.Yoga',
    ];
    return cfg;
  });

  config = withInfoPlist(config, (cfg) => {
    cfg.modResults['NSUserActivityTypes'] = [
      'com.apple.cocoaidentity.tokens',
    ];
    return cfg;
  });

  return config;
};

---

## STEP 3 — Create eas.json

CREATE the file artifacts/mobile/eas.json:

{
  "cli": {
    "version": ">= 12.0.0",
    "appVersionSource": "remote"
  },
  "build": {
    "development": {
      "developmentClient": true,
      "distribution": "internal",
      "ios": {
        "simulator": false,
        "buildConfiguration": "Debug",
        "resourceClass": "m-medium"
      }
    },
    "preview": {
      "distribution": "internal",
      "ios": {
        "buildConfiguration": "Release",
        "resourceClass": "m-medium"
      }
    },
    "production": {
      "ios": {
        "buildConfiguration": "Release",
        "resourceClass": "m-medium"
      }
    }
  },
  "submit": {
    "production": {
      "ios": {
        "appleId": "YOUR_APPLE_ID@email.com",
        "ascAppId": "YOUR_APP_STORE_CONNECT_APP_ID",
        "appleTeamId": "YOUR_TEAM_ID"
      }
    }
  }
}

---

## STEP 4 — Fix font names in app/_layout.tsx

FILE: artifacts/mobile/app/_layout.tsx

The @expo-google-fonts/dm-sans package exports DMSans_* names.
The import uses DM_Sans_* which causes TypeScript errors.

Replace this import block:
import {
  DM_Sans_400Regular,
  DM_Sans_500Medium,
  DM_Sans_600SemiBold,
  DM_Sans_700Bold,
  useFonts,
} from "@expo-google-fonts/dm-sans";

With:
import {
  DMSans_400Regular,
  DMSans_500Medium,
  DMSans_600SemiBold,
  DMSans_700Bold,
  useFonts,
} from "@expo-google-fonts/dm-sans";

Replace the useFonts call:
  const [fontsLoaded, fontError] = useFonts({
    DM_Sans_400Regular,
    DM_Sans_500Medium,
    DM_Sans_600SemiBold,
    DM_Sans_700Bold,
  });

With (using string keys so all StyleSheet fontFamily 
references continue working without any other changes):
  const [fontsLoaded, fontError] = useFonts({
    "DM_Sans_400Regular": DMSans_400Regular,
    "DM_Sans_500Medium": DMSans_500Medium,
    "DM_Sans_600SemiBold": DMSans_600SemiBold,
    "DM_Sans_700Bold": DMSans_700Bold,
  });

Add Linking import at top of file (after existing imports):
import { Linking } from 'react-native';

Add deep link handler inside RootNavigator component,
AFTER the existing onboarding redirect useEffect:

  const handleDeepLink = (url: string) => {
    if (!url) return;
    try {
      if (url.includes('stretch/session')) {
        const parts = url.split('?');
        const params: Record<string, string> = {};
        if (parts[1]) {
          parts[1].split('&').forEach(p => {
            const [k, v] = p.split('=');
            if (k && v) params[k] = decodeURIComponent(v);
          });
        }
        router.push({ pathname: '/stretch/session', params });
      }
    } catch (e) {
      console.warn('[SGDeepLink] error:', e);
    }
  };

  useEffect(() => {
    Linking.getInitialURL().then((url) => {
      if (url) handleDeepLink(url);
    });
    const sub = Linking.addEventListener('url', ({ url }) => {
      handleDeepLink(url);
    });
    return () => sub.remove();
  }, []);

---

## STEP 5 — Create native/StretchGateNative.ts

CREATE directory artifacts/mobile/native/ if needed.
CREATE file artifacts/mobile/native/StretchGateNative.ts:

import { NativeModules, Platform } from 'react-native';

const { StretchGateNative: NativeModule } = NativeModules;
const isAvailable = Platform.OS === 'ios' && !!NativeModule;

export const StretchGateNative = {
  requestAuthorization: async (): Promise<boolean> => {
    if (!isAvailable) {
      console.log('[SGNative] MOCK requestAuthorization');
      await new Promise(r => setTimeout(r, 800));
      return true;
    }
    return NativeModule.requestAuthorization();
  },

  presentAppPicker: async (): Promise<string> => {
    if (!isAvailable) {
      console.log('[SGNative] MOCK presentAppPicker');
      await new Promise(r => setTimeout(r, 500));
      return JSON.stringify({ mock: true });
    }
    return NativeModule.presentAppPicker();
  },

  saveSelection: (selectionJson: string): void => {
    if (!isAvailable) {
      console.log('[SGNative] MOCK saveSelection');
      return;
    }
    NativeModule.saveSelection(selectionJson);
  },

  applyRestrictions: (): void => {
    if (!isAvailable) {
      console.log('[SGNative] MOCK applyRestrictions');
      return;
    }
    NativeModule.applyRestrictions();
  },

  liftRestrictions: (): void => {
    if (!isAvailable) {
      console.log('[SGNative] MOCK liftRestrictions');
      return;
    }
    NativeModule.liftRestrictions();
  },

  clearAllRestrictions: (): void => {
    if (!isAvailable) {
      console.log('[SGNative] MOCK clearAllRestrictions');
      return;
    }
    NativeModule.clearAllRestrictions();
  },

  getAuthorizationStatus: async (): Promise
    'authorized' | 'denied' | 'notDetermined'
  > => {
    if (!isAvailable) {
      console.log('[SGNative] MOCK getAuthorizationStatus');
      return 'notDetermined';
    }
    return NativeModule.getAuthorizationStatus();
  },
};

export default StretchGateNative;

---

## STEP 6 — Create services/notifications.ts

CREATE directory artifacts/mobile/services/ if needed.
CREATE file artifacts/mobile/services/notifications.ts:

import * as Notifications from 'expo-notifications';
import { Platform } from 'react-native';

Notifications.setNotificationHandler({
  handleNotification: async () => ({
    shouldShowAlert: true,
    shouldPlaySound: false,
    shouldSetBadge: false,
  }),
});

export interface NotificationTime {
  hour: number;
  minute: number;
}

const IDS = {
  DAILY: 'sg-daily-reminder',
  STREAK: 'sg-streak-alert',
};

export async function requestNotificationPermission(): Promise<boolean> {
  if (Platform.OS === 'web') return false;
  const { status: existing } = await Notifications.getPermissionsAsync();
  if (existing === 'granted') return true;
  const { status } = await Notifications.requestPermissionsAsync();
  return status === 'granted';
}

export async function scheduleDailyReminder(
  time: NotificationTime,
  currentStreak: number
): Promise<void> {
  if (Platform.OS === 'web') return;
  await Notifications.cancelScheduledNotificationAsync(
    IDS.DAILY
  ).catch(() => {});

  const body =
    currentStreak > 0
      ? `🔥 ${currentStreak} day streak — keep it going`
      : 'Time to move. Your body will thank you.';

  await Notifications.scheduleNotificationAsync({
    identifier: IDS.DAILY,
    content: {
      title: 'StretchGate',
      body,
      sound: false,
      data: { type: 'daily_reminder' },
    },
    trigger: {
      type: Notifications.SchedulableTriggerInputTypes.DAILY,
      hour: time.hour,
      minute: time.minute,
    },
  });
}

export async function scheduleStreakAlert(streak: number): Promise<void> {
  if (Platform.OS === 'web' || streak === 0) return;
  await Notifications.cancelScheduledNotificationAsync(
    IDS.STREAK
  ).catch(() => {});

  await Notifications.scheduleNotificationAsync({
    identifier: IDS.STREAK,
    content: {
      title: `${streak} day streak at risk 🔥`,
      body: 'Do one stretch before midnight to keep it alive.',
      sound: false,
      data: { type: 'streak_alert' },
    },
    trigger: {
      type: Notifications.SchedulableTriggerInputTypes.DAILY,
      hour: 20,
      minute: 0,
    },
  });
}

export async function cancelAllNotifications(): Promise<void> {
  if (Platform.OS === 'web') return;
  await Notifications.cancelAllScheduledNotificationsAsync();
}

export async function handleSessionCompleted(
  streak: number
): Promise<void> {
  if (Platform.OS === 'web') return;
  await Notifications.cancelScheduledNotificationAsync(
    IDS.STREAK
  ).catch(() => {});
  await scheduleStreakAlert(streak + 1);
}

---

## STEP 7 — Update AppContext.tsx

FILE: artifacts/mobile/context/AppContext.tsx

ADD these imports at the top after existing imports:
import {
  scheduleDailyReminder,
  scheduleStreakAlert,
  cancelAllNotifications,
  handleSessionCompleted,
} from '@/services/notifications';

ADD reminderTime to AppSettings interface:
  reminderTime?: { hour: number; minute: number };

ADD to DEFAULT_SETTINGS:
  reminderTime: { hour: 9, minute: 0 },

REPLACE updateSettings with:
  const updateSettings = useCallback(async (
    updates: Partial<AppSettings>
  ) => {
    setSettings(prev => {
      const next = { ...prev, ...updates };
      AsyncStorage.setItem(
        STORAGE_KEYS.SETTINGS,
        JSON.stringify(next)
      );
      if ('reminderEnabled' in updates || 'reminderTime' in updates) {
        if (next.reminderEnabled) {
          const time = next.reminderTime ?? { hour: 9, minute: 0 };
          const streak = calculateStreak(sessions);
          scheduleDailyReminder(time, streak).catch(() => {});
          scheduleStreakAlert(streak).catch(() => {});
        } else {
          cancelAllNotifications().catch(() => {});
        }
      }
      return next;
    });
  }, [sessions]);

REPLACE recordSession with:
  const recordSession = useCallback(async (
    session: Omit<StretchSession, 'id' | 'completedAt'>
  ) => {
    const now = new Date().toISOString();
    const oneMinAgo = new Date(Date.now() - 60000).toISOString();

    setSessions(prev => {
      const recentDupe = prev.find(
        s =>
          s.stretchId === session.stretchId &&
          s.completedAt > oneMinAgo
      );
      if (recentDupe) return prev;

      const newSession: StretchSession = {
        ...session,
        id:
          Date.now().toString() +
          Math.random().toString(36).substr(2, 9),
        completedAt: now,
      };
      const newSessions = [newSession, ...prev];
      AsyncStorage.setItem(
        STORAGE_KEYS.SESSIONS,
        JSON.stringify(newSessions)
      );
      return newSessions;
    });

    try {
      const streak = calculateStreak(sessions);
      await handleSessionCompleted(streak);
    } catch (e) {
      console.warn('[SGNotif] reschedule failed:', e);
    }
  }, [sessions]);

---

## STEP 8 — Update session.tsx

FILE: artifacts/mobile/app/stretch/session.tsx

ADD import at top:
import { StretchGateNative } from '@/native/StretchGateNative';

REPLACE handleComplete with:
  const handleComplete = async () => {
    if (!stretch) return;

    if (targetApp) {
      try {
        StretchGateNative.liftRestrictions();
      } catch (e) {
        console.warn('[SGNative] liftRestrictions error:', e);
      }
    }

    await recordSession({
      stretchId: stretch.id,
      stretchName: stretch.name,
      durationSeconds: elapsed,
      targetApp,
    });

    router.back();
  };

---

## STEP 9 — Update onboarding.tsx

FILE: artifacts/mobile/app/onboarding.tsx

ADD import at top:
import { StretchGateNative } from '@/native/StretchGateNative';

FIND requestFamilyControlsAuth inside PermissionsStep and REPLACE:
  const requestFamilyControlsAuth = async (): Promise<boolean> => {
    return StretchGateNative.requestAuthorization();
  };

---

## STEP 10 — Update settings.tsx

FILE: artifacts/mobile/app/(tabs)/settings.tsx

ADD imports at top:
import { StretchGateNative } from '@/native/StretchGateNative';
import {
  requestNotificationPermission,
  scheduleDailyReminder,
  cancelAllNotifications,
} from '@/services/notifications';

REPLACE the entire SCREEN TIME section Animated.View with:

        <Animated.View entering={FadeInDown.duration(380)}>
          <Section title="SCREEN TIME">
            <View style={styles.stCard}>
              <View style={styles.stHeader}>
                <View
                  style={[
                    styles.stIconWrap,
                    { backgroundColor: Colors.primaryMuted },
                  ]}
                >
                  <Ionicons
                    name="shield-checkmark-outline"
                    size={22}
                    color={Colors.primary}
                  />
                </View>
                <View style={{ flex: 1 }}>
                  <Text style={styles.stTitle}>App Gating</Text>
                  <Text style={styles.stBody}>
                    StretchGate uses Apple Family Controls to
                    require a stretch before opening your chosen
                    apps.
                  </Text>
                </View>
              </View>
              <ScreenTimeStatusRow done={settings.isLockActive} />
              <Pressable
                style={[
                  styles.stBtn,
                  { backgroundColor: Colors.primary },
                ]}
                onPress={handleScreenTimeSetup}
              >
                <Ionicons
                  name="shield-checkmark-outline"
                  size={15}
                  color={Colors.white}
                />
                <Text style={styles.stBtnText}>
                  {settings.isLockActive
                    ? 'Reconfigure Gated Apps'
                    : 'Set Up App Gating'}
                </Text>
              </Pressable>
              {settings.isLockActive && (
                <Pressable
                  style={[
                    styles.stBtn,
                    {
                      backgroundColor: Colors.errorMuted,
                      marginTop: 8,
                    },
                  ]}
                  onPress={handleDisableLock}
                >
                  <Ionicons
                    name="lock-open-outline"
                    size={15}
                    color={Colors.error}
                  />
                  <Text
                    style={[
                      styles.stBtnText,
                      { color: Colors.error },
                    ]}
                  >
                    Disable All Gating
                  </Text>
                </Pressable>
              )}
            </View>
          </Section>
        </Animated.View>

ADD a NOTIFICATIONS section right after Screen Time section 
and before MINDFUL GATES section:

        <Animated.View entering={FadeInDown.duration(380).delay(55)}>
          <Section title="NOTIFICATIONS">
            <Row
              icon="notifications-outline"
              iconBg={Colors.accentMuted}
              iconColor={Colors.accent}
              label="Daily reminder"
              sub={
                settings.reminderEnabled && settings.reminderTime
                  ? `Fires at ${String(
                      settings.reminderTime.hour
                    ).padStart(2, '0')}:${String(
                      settings.reminderTime.minute
                    ).padStart(2, '0')}`
                  : 'Off'
              }
              divider={!!settings.reminderEnabled}
              right={
                <Switch
                  value={settings.reminderEnabled}
                  onValueChange={handleToggleReminder}
                  trackColor={{
                    false: Colors.bgSurface,
                    true: Colors.primary,
                  }}
                  thumbColor={Colors.white}
                  ios_backgroundColor={Colors.bgSurface}
                />
              }
            />
            {settings.reminderEnabled && (
              <Row
                icon="time-outline"
                iconBg={Colors.primaryMuted}
                iconColor={Colors.primary}
                label="Reminder time"
                value={
                  settings.reminderTime
                    ? `${String(
                        settings.reminderTime.hour
                      ).padStart(2, '0')}:${String(
                        settings.reminderTime.minute
                      ).padStart(2, '0')}`
                    : '09:00'
                }
                onPress={handleChangeReminderTime}
                divider={false}
              />
            )}
          </Section>
        </Animated.View>

ADD these handler functions inside SettingsScreen component
after the existing toggleApp, toggleArea handlers:

  const handleScreenTimeSetup = async () => {
    if (Platform.OS !== 'web')
      await Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Medium);
    try {
      const authorized =
        await StretchGateNative.requestAuthorization();
      if (authorized) {
        await updateSettings({ isLockActive: true });
        StretchGateNative.applyRestrictions();
      }
    } catch (e) {
      Alert.alert(
        'Setup Failed',
        'Could not set up app gating. Make sure Screen Time permission is granted in Settings.',
        [{ text: 'OK' }]
      );
    }
  };

  const handleDisableLock = () => {
    Alert.alert(
      'Disable App Gating',
      'This removes all stretch requirements. Are you sure?',
      [
        { text: 'Cancel', style: 'cancel' },
        {
          text: 'Disable',
          style: 'destructive',
          onPress: async () => {
            StretchGateNative.clearAllRestrictions();
            await updateSettings({ isLockActive: false });
          },
        },
      ]
    );
  };

  const handleToggleReminder = async (value: boolean) => {
    if (Platform.OS !== 'web') await Haptics.selectionAsync();
    if (value) {
      const granted = await requestNotificationPermission();
      if (!granted) {
        Alert.alert(
          'Notifications Blocked',
          'Enable notifications for StretchGate in Settings.',
          [
            {
              text: 'Open Settings',
              onPress: () => Linking.openSettings(),
            },
            { text: 'Cancel', style: 'cancel' },
          ]
        );
        return;
      }
    }
    await updateSettings({ reminderEnabled: value });
  };

  const handleChangeReminderTime = () => {
    Alert.alert(
      'Reminder Time',
      'When should we remind you to stretch?',
      [
        {
          text: '7:00 AM',
          onPress: () =>
            updateSettings({
              reminderTime: { hour: 7, minute: 0 },
            }),
        },
        {
          text: '8:00 AM',
          onPress: () =>
            updateSettings({
              reminderTime: { hour: 8, minute: 0 },
            }),
        },
        {
          text: '9:00 AM',
          onPress: () =>
            updateSettings({
              reminderTime: { hour: 9, minute: 0 },
            }),
        },
        {
          text: '12:00 PM',
          onPress: () =>
            updateSettings({
              reminderTime: { hour: 12, minute: 0 },
            }),
        },
        {
          text: '6:00 PM',
          onPress: () =>
            updateSettings({
              reminderTime: { hour: 18, minute: 0 },
            }),
        },
        {
          text: '8:00 PM',
          onPress: () =>
            updateSettings({
              reminderTime: { hour: 20, minute: 0 },
            }),
        },
        { text: 'Cancel', style: 'cancel' },
      ]
    );
  };

REMOVE: const [screenTimeDone, setScreenTimeDone] = useState(false);
REMOVE: the openScreenTime function entirely

---

## STEP 11 — Write Swift files

CREATE these files. These go into the ios/ folder which will be
generated by expo prebuild on the Mac. Write them now so they
are ready in the repo.

CREATE artifacts/mobile/ios-native/StretchGateNative.swift:

import Foundation
import FamilyControls
import ManagedSettings
import DeviceActivity
import SwiftUI

@objc(StretchGateNative)
class StretchGateNative: NSObject {

  private let store = ManagedSettingsStore()
  private let activityCenter = DeviceActivityCenter()

  // Load saved selection from App Group shared container
  private func loadSavedSelection() -> FamilyActivitySelection? {
    let defaults = UserDefaults(suiteName: "group.com.zorockz.Yoga")
    guard
      let data = defaults?.string(forKey: "savedSelection"),
      let encoded = data.data(using: .utf8),
      let selection = try? JSONDecoder().decode(
        FamilyActivitySelection.self, from: encoded
      )
    else { return nil }
    return selection
  }

  @objc func requestAuthorization(
    _ resolve: @escaping RCTPromiseResolveBlock,
    rejecter reject: @escaping RCTPromiseRejectBlock
  ) {
    Task {
      do {
        try await AuthorizationCenter.shared
          .requestAuthorization(for: .individual)
        DispatchQueue.main.async { resolve(true) }
      } catch {
        DispatchQueue.main.async {
          reject("AUTH_ERROR", error.localizedDescription, error)
        }
      }
    }
  }

  @objc func applyRestrictions() {
    guard let selection = loadSavedSelection(),
          !selection.applicationTokens.isEmpty else {
      print("[StretchGate] No saved selection to apply")
      return
    }
    store.shield.applications = selection.applicationTokens
    print("[StretchGate] Shield applied to",
          selection.applicationTokens.count, "apps")
  }

  @objc func liftRestrictions() {
    // Since user wants scheduled locking only (no unlock window),
    // this just clears the shield after a completed stretch.
    // The DeviceActivityMonitor will re-apply on schedule.
    store.clearAllSettings()
    print("[StretchGate] Shield lifted")
  }

  @objc func clearAllRestrictions() {
    store.clearAllSettings()
    let name = DeviceActivityName("stretchgate.schedule")
    activityCenter.stopMonitoring([name])
    print("[StretchGate] All restrictions cleared")
  }

  @objc func saveSelection(_ selectionData: String) {
    let defaults = UserDefaults(suiteName: "group.com.zorockz.Yoga")
    defaults?.set(selectionData, forKey: "savedSelection")
    defaults?.synchronize()
    print("[StretchGate] Selection saved")
    // Apply restrictions immediately after saving
    applyRestrictions()
  }

  @objc func getAuthorizationStatus(
    _ resolve: @escaping RCTPromiseResolveBlock,
    rejecter reject: @escaping RCTPromiseRejectBlock
  ) {
    let status = AuthorizationCenter.shared.authorizationStatus
    switch status {
    case .approved:  DispatchQueue.main.async { resolve("authorized") }
    case .denied:    DispatchQueue.main.async { resolve("denied") }
    default:         DispatchQueue.main.async { resolve("notDetermined") }
    }
  }

  @objc static func requiresMainQueueSetup() -> Bool { return false }
}


CREATE artifacts/mobile/ios-native/StretchGateNative.m:

#import <React/RCTBridgeModule.h>

@interface RCT_EXTERN_MODULE(StretchGateNative, NSObject)

RCT_EXTERN_METHOD(
  requestAuthorization:(RCTPromiseResolveBlock)resolve
  rejecter:(RCTPromiseRejectBlock)reject
)

RCT_EXTERN_METHOD(applyRestrictions)

RCT_EXTERN_METHOD(liftRestrictions)

RCT_EXTERN_METHOD(clearAllRestrictions)

RCT_EXTERN_METHOD(saveSelection:(NSString *)selectionData)

RCT_EXTERN_METHOD(
  getAuthorizationStatus:(RCTPromiseResolveBlock)resolve
  rejecter:(RCTPromiseRejectBlock)reject
)

@end


CREATE artifacts/mobile/ios-native/DeviceActivityMonitorExtension.swift:

import DeviceActivity
import ManagedSettings

class DeviceActivityMonitorExtension: DeviceActivityMonitor {

  let store = ManagedSettingsStore()

  private func loadSavedSelection() -> FamilyActivitySelection? {
    let defaults = UserDefaults(
      suiteName: "group.com.zorockz.Yoga"
    )
    guard
      let data = defaults?.string(forKey: "savedSelection"),
      let encoded = data.data(using: .utf8),
      let selection = try? JSONDecoder().decode(
        FamilyActivitySelection.self, from: encoded
      )
    else { return nil }
    return selection
  }

  // Called when a scheduled interval starts
  // This is when we lock the apps again
  override func intervalDidStart(
    for activity: DeviceActivityName
  ) {
    guard activity.rawValue == "stretchgate.schedule" else {
      return
    }
    guard let selection = loadSavedSelection(),
          !selection.applicationTokens.isEmpty else {
      print("[SGMonitor] No selection found at interval start")
      return
    }
    store.shield.applications = selection.applicationTokens
    print("[SGMonitor] Shield applied at interval start:",
          selection.applicationTokens.count, "apps")
  }

  // Called when a scheduled interval ends
  override func intervalDidEnd(
    for activity: DeviceActivityName
  ) {
    print("[SGMonitor] Interval ended:", activity.rawValue)
  }
}


CREATE artifacts/mobile/ios-native/DeviceActivityMonitorExtension-Info.plist:

<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN"
  "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <key>CFBundleDevelopmentRegion</key>
  <string>en</string>
  <key>CFBundleDisplayName</key>
  <string>DeviceActivityMonitor</string>
  <key>CFBundleExecutable</key>
  <string>$(EXECUTABLE_NAME)</string>
  <key>CFBundleIdentifier</key>
  <string>com.zorockz.Yoga.DeviceActivityMonitor</string>
  <key>CFBundleInfoDictionaryVersion</key>
  <string>6.0</string>
  <key>CFBundleName</key>
  <string>$(PRODUCT_NAME)</string>
  <key>CFBundlePackageType</key>
  <string>XPC!</string>
  <key>CFBundleShortVersionString</key>
  <string>1.0</string>
  <key>CFBundleVersion</key>
  <string>1</string>
  <key>NSExtension</key>
  <dict>
    <key>NSExtensionPointIdentifier</key>
    <string>com.apple.deviceactivity.monitor</string>
    <key>NSExtensionPrincipalClass</key>
    <string>$(PRODUCT_MODULE_NAME).DeviceActivityMonitorExtension</string>
  </dict>
</dict>
</plist>


---

## STEP 12 — Create XCODE_SETUP.md

CREATE artifacts/mobile/XCODE_SETUP.md:

# Xcode Setup — StretchGate

## Bundle IDs
- Main app: com.zorockz.Yoga
- Extension: com.zorockz.Yoga.DeviceActivityMonitor
- App Group: group.com.zorockz.Yoga

## Step 1: Open project
open ios/StretchGate.xcworkspace
NEVER use .xcodeproj

## Step 2: Main app target (StretchGate)
1. Click blue project in left panel
2. Click StretchGate under TARGETS
3. Signing & Capabilities:
   - Uncheck "Automatically manage signing"
   - Team: your Apple Developer account
   - Bundle ID: com.zorockz.Yoga
   - Provisioning Profile: iOS App Development (com.zorockz.Yoga)
4. + Capability → Family Controls
5. + Capability → App Groups → add: group.com.zorockz.Yoga

## Step 3: Add extension target
1. File → New → Target
2. Search: Device Activity Monitor → Next
3. Product Name: DeviceActivityMonitor
4. Bundle ID: com.zorockz.Yoga.DeviceActivityMonitor
5. Language: Swift
6. Finish → Activate

## Step 4: Extension target setup
1. Click DeviceActivityMonitor under TARGETS
2. Signing & Capabilities:
   - Uncheck auto signing
   - Same team as main app
   - Bundle ID: com.zorockz.Yoga.DeviceActivityMonitor
   - Provisioning Profile: iOS App Development
     (com.zorockz.Yoga.DeviceActivityMonitor)
3. + Capability → Family Controls
4. + Capability → App Groups → group.com.zorockz.Yoga

## Step 5: Add Swift files

Native files are in: artifacts/mobile/ios-native/

Add to MAIN APP target (StretchGate):
- Right-click StretchGate folder in left panel
- Add Files to StretchGate
- Select: StretchGateNative.swift
- Select: StretchGateNative.m
- Check: Add to target = StretchGate
- If prompted about bridging header → Create Bridging Header

Add to EXTENSION target (DeviceActivityMonitor):
- Right-click DeviceActivityMonitor folder
- Add Files
- Select: DeviceActivityMonitorExtension.swift
- Check: Add to target = DeviceActivityMonitor

## Step 6: Download provisioning profiles
Xcode → Preferences → Accounts → your Apple ID
→ Download Manual Profiles

## Step 7: Clean and build
Product → Clean Build Folder (Shift+Cmd+K)
Select your iPhone in device picker
Press Cmd+R

## Step 8: Trust on iPhone
Settings → VPN & Device Management → Developer App
→ Trust com.zorockz.Yoga

## Step 9: Test
1. Open app → onboarding → Grant Screen Time Access
   → iOS permission sheet must appear
2. Settings → Set Up App Gating
   → FamilyActivityPicker must open
3. Select TikTok → Done
4. Press home → open TikTok
   → Shield screen must appear
5. Open StretchGate → do stretch → Log session
   → TikTok opens again
6. Open TikTok again
   → Shield appears again (no unlock window = always blocked)


---

## STEP 13 — TypeScript check

Run: cd artifacts/mobile && npx tsc --noEmit 2>&1

Fix ALL errors. Most common remaining issues:
1. Implicit any in filter/map — add : StretchSession type
2. Section missing children — make children?: React.ReactNode
3. key prop in interface — remove key from prop interfaces
4. onToggle type — use () => void | Promise<void>
5. Switch onChange implicit any — add (v: boolean) => ...

Report every error found and fix applied.
Target: 0 errors.

---

## STEP 14 — Commit and push

git add -A
git commit -m "feat: Family Controls integration — bundle ID fix, native bridge, Swift files, DeviceActivity extension, notifications, settings Screen Time UI, deep link handler, eas.json, app.config.js"
git push origin main

---

## FINAL REPORT

Report:
1. Every step completed ✓ or ✗
2. Any errors encountered and how resolved
3. TypeScript error count (target: 0)
4. Full list of files created or modified
5. Git commit hash
6. Reminder: expo prebuild + pod install + Xcode setup
   must be done on Mac — see XCODE_SETUP.md